import { Dimensions } from "react-native";

export const { width, height } = Dimensions.get("window");

export const HORIZONTAL_MARGIN = 20;

export const [GALLERY_IMAGE_HEIGHT, GALLERY_IMAGE_WIDTH, SLIDER_WIDTH] = [
  190,
  width,
  width
];

export const FILTER_BAR_HEIGHT = 75;

export const HEADER_IMAGE_HEIGHT = 230;

export const STATUS_BAR_OFFSET = 40;
